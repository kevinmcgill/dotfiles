# GENERATED FILE — do not edit directly.
# Edit config.base.yml for settings, theme-dark.yml / theme-light.yml for colors.
# Regenerated by dark-notify/theme-switch.sh on appearance change.

# lazygit base config (non-theme settings)
#
# DO NOT put theme colors here. Theme is in theme-dark.yml / theme-light.yml.
# The dark-notify script concatenates this file + the active theme file
# to generate config.yml. Edit THIS file for non-theme changes.

# Temporarily ignore the GPG requirement for specific features like interactive rebases
# and rewording commit messages
git:
  overrideGpg: true
gui:
  nerdFontsVersion: "3"
# lazygit light theme: Rose Pine Dawn
#
# This file is concatenated with config.base.yml by the dark-notify
# theme-switch.sh script to generate config.yml.
# Source: https://github.com/rose-pine/lazygit
#
# Well actually I use the default light theme for lazygit, which seems to inherit from the terminal colors.
