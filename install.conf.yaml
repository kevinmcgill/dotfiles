- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux: tmux/
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.config/nvim: nvim
    ~/.config/wezterm/wezterm.lua: wezterm.lua

- shell:
  - [git submodule update --init --recursive, Installing submodules]
