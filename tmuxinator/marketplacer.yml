name: marketplacer
root: ~/Sites/marketplacer/

windows:
  - git:
    - lazygit
  - code:
    - nvim .
  - docker:
    - docker compose up db elasticsearch redis cache
  - server:
    - bundle exec rails server -p 3000
  - yarn:
    - yarn start
  - sidekiq:
    - bundle exec sidekiq
  - terminal:
    - la

